package ru.gohasoft.wanderingtable.data.main.remote.api

/**
 * No screen in the app pages: the club's schedule, catalogue and news feed are all small and are
 * shown as a single scrollable list. One generous page stands in for pagination until a screen
 * actually needs it.
 */
internal const val DEFAULT_PAGE_SIZE = 100
