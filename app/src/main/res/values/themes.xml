<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- windowBackground is what the system paints between the splash handing off and
         Compose drawing its first frame — without it that gap flashes white. -->
    <style name="Theme.WanderingTable" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/wt_splash_background</item>
    </style>

    <!-- Launch theme. androidx.core:splashscreen backports this to minSdk 28. -->
    <style name="Theme.WanderingTable.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/wt_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.WanderingTable</item>
    </style>
</resources>
